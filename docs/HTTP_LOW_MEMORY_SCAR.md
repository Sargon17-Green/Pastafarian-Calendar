# Cicatrix memoriae HTTP apud Render

Die 2026-09-03 Render Free instantiarum mortes explicite rettulit: `Ran out of memory (used over 512MB) while running your code.` Transportus async iam petitionem longam a vita HTTP separavit; causa reliqua igitur vertex RSS ipsius calculi erat.

Investigatio monstri ostendit Patch 38 `legalWeavingSkeletonVault` tabulas `suffixPerFixedActive` ex multis `boost::multiprecision::cpp_int` persistentibus servare. Limen vetus 512 numerum skeletonum terminat, non bytes. Constructio quoque corpus locale in burialem persistentem duplicat. In deployment 512 MB hoc sepulcrum maius est quam paries.

Haec cicatrix **src/monster.cpp non mutat**. `build_http_api.sh`, tantum cum `PASTAFARI_HTTP_LOW_MEMORY_SCAR=1`, copiam temporalem monstri per `tools/http_low_memory_monster.awk` parat. In illa copia sola resurrectio et burial persistentia skeletonis Patch 38 clauduntur.

Algorithmus `FastLegalMonthWeavingCounterInternal` et tabula localis eius manent. Patch 40 funeral shortcut manet. Cetera sepulcra accelerationis manent. `calendarDateSpaghetti(c,t)`, Stage 56, Pair Tomb et output semanticum non mutantur. Nullus Stage 57 creatur.

Docker deployment cicatricem activat. Build ordinarium extra Docker eam non activat, ut corpus historicum et probationes directae intacta maneant. CI aedificat eadem configuratione humili memoria atque RSS processūs in smoke implicito metitur.

Hoc est remedium deploymentis ad operandum sub 512 MB, non consilium finale cache. Postquam servitium stabiliter complet, cache Patch 38 separatim redesignari potest (e.g. ownership immutabile commune / budget byte-based) sine hac necessitate mutandi semanticam.
