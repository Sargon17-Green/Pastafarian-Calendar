# Fixtures

No fixture de output expected es incluineda como un copia de un otra implementa. Pos la oracle G es creada, esta diretorio debe conteni fixtures jenerada local par LabVIEW/G sola.

La inputs minima per fixtures debe inclui Foundation, dias prosima a Foundation, casos de SAVE a `M`, `2M`, e un grupo de pares `(calculationDay,targetDay)` elejeda direta de la referensa normativa.
