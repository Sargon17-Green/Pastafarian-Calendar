# Delta — canonical saved-sum correction — JavaScript + Interlingue

Repository: `Sargon17-Green/Pastafarian-Calendar`  
Branch: `JavaScript+Interlingue`  
Expected HEAD from task: `78eb29ae0b1fd67ccbaf02e336482eafcc89d9d0`  
Observed and revalidated HEAD: `b8126dfe6a36f05fd6eb8c45479062e039412678`

The observed branch is 8 commits ahead of the expected HEAD. This delta is therefore based on the observed/revalidated HEAD and preserves those later changes, including Stage-57 Patch-26 behavior and Stage-58 acceleration machinery.

## Classification

Before this delta, the reachable Stage-56/57/58 sauce route used the noncanonical raw-sum detour: `R=SAVE(S+149*r)` selected the permutation but `u` added raw `S`. `tests/normative-reference.js` was already correct and was not changed.

After this delta, the reachable route is canonical saved-sum: the same saved `R` is used for the permutation and inside `u`. All six outputs of each round still read one old six-bowl snapshot and commit together.

The production symbol is now `stage56CanonicalSavedSumPostStir`. Old exported names containing `RawBowlSum` remain only as explicitly deprecated compatibility aliases to the canonical function. Raw-sum arithmetic itself remains only in `tests/stage-56-reference.js`, labelled `HISTORICAL — SUPERSEDED MUTANT ONLY` and used solely to prove that the discriminator kills the mutant.

## Discriminator

For old bowls `[1,2,3,4,5,6]` and stir 1:

- `S=21`
- `R=170`
- permutation `[2,4,1,3,6,5]`
- canonical `u` by position: `[209,190,208,223,236,240]`
- canonical bowls: `[43348,43821,49771,36114,57684,55801]`
- raw-sum mutant bowls: `[3565,3740,5518,1695,8365,7674]`

The real implementation matches canonical saved-sum and disagrees with the mutant.

## Differential evidence actually executed

The executed saved-sum tests cover bowls after visible drop 46, every one of the 12 post-stirs, final bowls, query streams, gate/year structure, cutlet partition/names, month lengths/names, final five-field dates, Foundation, `c=t`, `t<c`, `t>c`, negative gate indices, year/cutlet/month boundaries, the historical Patch-26 hard witness, deterministic random sauce corpus, cold/warm caches, repeated calls, A→B→A, import order, and failure→retry.

Canonical witnesses include:

- Foundation: `(5000,10,503,20,56)` by canonical indices.
- `c=t=-15048173`: `(5000,17,456,33,87)`.
- `t=c-1`: `(5000,17,455,41,97)`.
- `t=c+1`: `(5000,17,457,36,84)`.
- Patch-26 witness `(-15048553,-15044872)`: `(5000,5,347,31,123)`.

For the Patch-26 witness, the Stage-57 mechanism is preserved. With saved-sum inputs the semantic year and the Patch-26 round-trip now agree on Year 5000 `(-15049315,-15044808]`, gate indices 11..20, so the legacy guard passes. This is a downstream data change from the Sauce correction, not a rollback of Patch 26.

## Derived material and caches

`tests/fixtures/canonical-saved-sum-downstream.json` was generated from the independently audited normative reference path; production was not used as its oracle.

The following Stage-58 evidence files were regenerated by executing the corrected path:

- `artifacts/CANONICAL_SAVED_SUM_VERIFICATION.json`
- `artifacts/STAGE_58_SAUCE_PROBE.json`
- `artifacts/STAGE_58_GATE_CHECKPOINT_PROBE.json`
- `artifacts/STAGE_58_MEMORY_PROBE.json`
- `artifacts/STAGE_58_FINAL_VERIFICATION_LOG.txt`

The old Stage-58 baseline/after benchmark JSONL files and benchmark report were based on raw-sum semantics. They are deleted rather than search-and-replaced or presented as current performance evidence. `STAGE_58_DELTA_MANIFEST.txt` is also deleted because its old hashes describe the superseded evidence set.

Stage-58 runtime semantic memories are process-local/weak/bounded rather than persistent repository cache files. The corrected path was exercised cold/warm, repeat, A→B→A, import-order and failure→retry. Browser build IDs are source-fingerprint-derived, so a corrected browser rebuild will receive a new build identity rather than reuse the old generated core bytes.

## Historical material

`STAGE_56_CORRECTIVE_REPORT.md`, `STAGE_57_CORRECTIVE_REPORT.md`, and `STAGE_58_ACCELERATION_REPORT.md` are retained as prominent supersession markers that record the original base HEAD/blob identities and explain which old claims are no longer canonical. `SPAGHETTI_DEVELOPMENT_HISTORY.md` is intentionally left unchanged as archaeology and is explicitly declared non-authoritative in `DEVELOPMENT_STAGE.md`.

## Tests actually run

See `artifacts/STAGE_58_FINAL_VERIFICATION_LOG.txt` for the exact run record. All PASS claims there correspond to commands actually executed in this session. An aggregate long run timed out after earlier shards had passed; later heavy shards were rerun separately, and no PASS claim relies on the timed-out aggregate.

## Prepared but not run / remaining uncertainty

The complete checked-out branch and browser source tree were not available inside the local sandbox, and the authorized remote computer was offline. Therefore the following were not executed after the correction: full historical `npm test`, `tests/verify-stage-01.js`, `tests/browser-interface-all.js`, `scripts/build-browser.js`, `tests/browser-built-artifacts.js`, and an actual corrected GitHub Pages smoke test. No checked-in `browser/dist` artifact exists at the observed tree; Pages artifacts are generated by CI.

The branch's `tests/browser-cooking-component.js` contains a synthetic presentation-only sample trace that still names the old raw-sum semantic profile in the observed base. It was audited as non-oracle mock data but could not be safely rewritten without the complete source file in the sandbox. Core, normative trace, and canonical differential tests in this delta do not depend on that mock. This is the remaining known textual cleanup item.

No GitHub push, commit, release, branch write, or history mutation was performed.
