# היסטוריית פיתוח הספגטי

## שלב 1 — אתחול

הקו התחיל מאפס. לא נעשה שימוש בקוד, בדיקות, fixtures, outputs, hashes, logs, traces או artifacts של מימוש אחר.

נבנו רק שכבות כלליות וניטרליות: `MonsterContext`, מנהל בסיסי, dispatcher בסיסי, אימות בסיסי, מעטפת שגיאות ומעטפת metrics. שכבות אלה אינן מכילות ידע על 26 הטלאים ואינן משנות semantics.

נבנה oracle נקי ב־Dart מתוך הנספח הנורמטיבי המשובץ במשימה. ה־oracle הוא לצורכי בדיקה בלבד ואינו נקרא מתוך production.

נבנה `SourceLanguageCatalog` יחיד וקפוא עם 17 שמות קציצות ו־47 שמות חודשים, כשהסדר נקבע רק לפי `canonicalIndex`.

בדיקות ההרצה של השלב טרם אושרו בסביבת העבודה הנוכחית משום ש־Dart SDK אינו מותקן בה. אין לעבור לשלב 2 לפני הרצה ירוקה של `dart run bin/stage1_tests.dart` באותו קו־מימוש.

## אימות שלב 1 — 2026-08-27

בוצע ניסיון verification ממוקד לשלב 1 בלבד. נבדקו דרישת גרסת ה־SDK, היעדר זליגה גלויה של סמלי טלאים עתידיים, וזמינות כלי Dart בסביבה.

ה־SDK המתאים שנבחר הוא Dart stable `3.13.2`, התואם לטווח `>=3.0.0 <4.0.0` שב־`pubspec.yaml`. בפועל לא נמצא `dart` או `flutter` בסביבה, ארכיון ה־SDK הרשמי לא היה נגיש עקב כשל DNS, ואין חבילת Dart זמינה במטמון המקומי. לכן formatter, analyzer ו־`dart run bin/stage1_tests.dart` לא הורצו.

הסריקה אחר סמלי הטלאים העתידיים בקבצי production, harness וכלי העזר החזירה `NO_MATCHES`. לא נוסף קוד חדש ולא בוצע תיקון סמנטי.

האימות נשאר `INCOMPLETE`. `LAST_COMPLETED_STAGE` נשאר 0, ואין לעבור לשלב 2 לפני אימות Dart ירוק בפועל. פירוט מלא שמור ב־`TEST_LOG_STAGE_01.md`.
