# 意大利面怪物开发历史

## 第 1 阶段 — 引导

本实现线从空目录开始。此阶段只建立中性的基础设施，不写入任何未来历史缺陷、补丁、兼容开关或专用绕行路径。

已建立的怪物层只有：基础上下文、基础调度器、基础验证器、基础错误边界和基础指标外壳。这些层不参与规范计算，也不改变任何结果。

同时建立了冻结的简体中文 `SourceLanguageCatalog`、本地任意精度整数服务、独立测试 oracle 和测试框架。oracle 的职责是直接实现任务内嵌的 Appendix A；生产路径不得调用 oracle。

第 1 阶段静态审查中发现并修正了若干 Bootstrap 自身问题，包括任意精度整数的九位块解析、乘法符号、碗更新平方位置、排列秩边界、COBOL `CONTINUE` 语义、编织 DP 索引和若干按引用传递的字段表示不一致。这些都是第 1 阶段基础设施/oracle 的修正，不是第 01 至 26 个历史补丁中的任何一个。

26 个历史缺陷的“曾经如何理解、后来发现什么、如何绕过、为何等价”等内容现在全部留空；只有到对应的 DISCOVERY 或 PATCH 阶段才允许填写。

当前阶段不能宣布完成：本环境缺少 GnuCOBOL，测试尚未实际运行；测试 oracle 的堆所有权审查仍有已知限制；本工作序列也已经发生严格来源规则所禁止的非 COBOL 运行时调用。详细记录见 `docs/STAGE_01_STATIC_AUDIT.md`。
