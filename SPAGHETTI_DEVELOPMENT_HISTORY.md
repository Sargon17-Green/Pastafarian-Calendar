# ประวัติการพัฒนาแบบสปาเกตตี

## ระยะที่ 1 — การเริ่มต้น

สร้างสายการพัฒนาใหม่จากศูนย์สำหรับ Pony + ภาษาไทย สร้างเฉพาะโครงสร้างพื้นฐานทั่วไปที่เป็นกลาง ได้แก่ บริบทพื้นฐาน ตัวกระจายงานพื้นฐาน การตรวจสอบข้อผิดพลาด และโครงสำหรับตัววัดกับบันทึกที่ไม่เป็นอินพุตเชิงความหมาย

ยังไม่มีสมมติฐานเดิมที่ผิด ยังไม่มีแพตช์ และยังไม่มีชั้นความเข้ากันได้ที่เป็นของแพตช์ใดโดยเฉพาะ

### การยืนยันการเป็นเจ้าของสถานะ

เพิ่มการตรวจการเป็นเจ้าของให้กับโครงสร้างพื้นฐานโดยไม่เพิ่มตรรกะของระยะอนาคต `MonsterContext` ยังคงเป็นสถานะต่อคำขอภายใน ``actor`` เดียว ส่วนข้อมูลที่ข้าม ``actor`` ถูกจำกัดเป็น `MonsterSnapshot val` ที่ไม่อ้างถึง โครงเก็บข้อมูล ที่เปลี่ยนค่าได้ของบริบท ชุดทดสอบตรวจการไม่แชร์ อาร์เรย์, แมป, หน่วยจำผล และที่เก็บประตูระหว่างคำขอหรือระหว่าง อินสแตนซ์

### การแก้ข้อบกพร่องภายในระยะเริ่มต้นก่อนส่งมอบ

ระหว่างการตรวจระยะที่ 1 พบว่าการคูณ `BigInt` รุ่นร่างสะสมค่าของแต่ละหลักไว้ใน `U32` สำหรับอินพุตที่ยาวมากพอ ตัวสะสมนั้นสามารถล้นได้ จึงเปลี่ยนการคูณเป็นการสร้างผลคูณย่อยทีละหลักและบวกแบบเลขฐานสิบที่แม่นยำ โดยไม่มีตัวสะสมที่ขนาดค่าโตตามจำนวนหลัก

พบว่าออราเคิลซึ่งอยู่คนละแพ็กเกจเคยเรียก ตัวสร้าง ส่วนตัว `BigInt._parts` ซึ่งไม่สามารถเรียกข้ามแพ็กเกจใน Pony ได้ จึงเพิ่ม ตัวสร้าง สาธารณะ `BigInt.from_parts` และเปลี่ยนการอ้างอิงข้ามแพ็กเกจทั้งหมดให้ใช้ทางสาธารณะ

พบอีกว่าการสร้าง `String ref` หลายจุดตั้งใจคืนค่า `String val` โดยยังไม่ได้ยกสิทธิ์อ้างอิงอย่างถูกต้อง จุดที่สร้างข้อความแล้วต้องออกเป็นค่าที่อ่านอย่างเดียวจึงถูกครอบด้วย `recover val` ส่วนกุญแจของการนับการสานซึ่งอ่านจากสถานะที่เปลี่ยนค่าได้ถูกเปลี่ยนให้ประกอบข้อความแบบไม่ส่ง การอ้างอิง ที่เขียนได้ออกจากขอบเขต

สุดท้ายพบว่าการทดสอบข้าม ``actor`` ของสแนปช็อตเรียก `complete_action` สองครั้งทั้งที่ประกาศความคาดหวังไว้หนึ่งครั้ง จึงลดให้เหลือหนึ่งครั้งเพื่อให้การทดสอบวัดขอบเขตสิทธิ์อ้างอิงจริงโดยไม่ล้มเพราะตัวทดสอบเอง
