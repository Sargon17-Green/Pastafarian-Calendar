# Canonical saved-sum correction — JavaScript + Interlingue

Authority: the current Scroll. This correction is semantic only; it does not advance an unrelated development stage.

## Canonical rule

For each of the 12 final post-stirs, one six-bowl snapshot is taken. With `r=1..12`:

```text
S = sum(oldBowls)
R = SAVE(S + 149*r)
permutationRank = 1 + ((R - 1) mod 720)
u = old[B] + 3*old[P] + 5*old[N] + R + r + position^2
new[B] = SAVE(u^2 + 7*old[P]*old[N])
```

All six `new[B]` values read the same `oldBowls` snapshot and are committed together.

The historical Stage-56 raw-sum interpretation (`+S` inside `u`) is rejected. It remains only in `tests/stage-56-reference.js` as an explicitly labelled mutant used by the discriminator.

## Reachable route

`src/index.js` now exposes `stage56CanonicalSavedSumPostStir` and the reachable Stage-56/57/58 sauce path calls canonical saved-sum code. Old `RawBowlSum` export names remain only as compatibility aliases to the canonical implementation; their comments state that they do not denote raw-sum semantics.

`src/normative-cooking-trace.js` identifies the trace as `PASTAFARIAN_STAGE57_CANONICAL_SAVED_SUM` and reports `stage56SavedSumApplied=true`, `stage56RawBowlSumApplied=false`.

## Discriminator

Input bowls `[1,2,3,4,5,6]`, stir 1:

- raw `S = 21`
- saved `R = 170`
- permutation `[2,4,1,3,6,5]`
- canonical final bowls `[43348,43821,49771,36114,57684,55801]`
- raw-sum mutant bowls `[3565,3740,5518,1695,8365,7674]`

The production implementation matches the canonical calculation and disagrees with the mutant.

## Downstream witnesses

Canonical saved-sum outputs include:

- Foundation: `(5000,10,503,20,56)` by canonical indices.
- `c=t=-15048173`: `(5000,17,456,33,87)`.
- `t=c-1`: `(5000,17,455,41,97)`.
- `t=c+1`: `(5000,17,457,36,84)`.
- historical Patch-26 witness `(-15048553,-15044872)`: `(5000,5,347,31,123)`.

For the Patch-26 witness the later Stage-57 mechanism remains present. Under corrected saved-sum inputs, the sequential semantic year and the Patch-26 round-trip agree on Year 5000 `(-15049315,-15044808]`, gate indices 11..20; the legacy guard therefore passes instead of requiring the mismatch-preservation detour.

## Derived evidence

`tests/fixtures/canonical-saved-sum-downstream.json` was generated from the independent normative reference, not from production. Stage-58 sauce/gate/memory probes were regenerated by executing the corrected canonical path. Historical raw-sum benchmark files are removed by this delta rather than presented as current performance evidence.
